import os
from pathlib import Path
from dotenv import load_dotenv

load_dotenv()  # optional, for local dev .env

BASE_DIR = Path(__file__).resolve().parent.parent
SECRET_KEY = "replace-me"
DEBUG = True
ALLOWED_HOSTS = ["*"]

# other usual settings...
